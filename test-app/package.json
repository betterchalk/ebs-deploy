{
  "name": "ebs-deploy-app",
  "version": "1.0.0",
  "description": "Hello World in Node with Express",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "docker:build": "docker build . -t ebs-deploy-app",
    "docker:run": "docker run -p 49160:8080 -d ebs-deploy-app"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
